<!-- component -->
<nav class="bg-malibu-950 px-2 sm:px-4 py-5 shadow sticky top-0 z-50">
  <div class="container md:flex justify-between items-center mx-auto text-primary-bg-50">
    <a href="/" class="md:flex items-center hidden">
      <span class="self-center text-xl font-semibold whitespace-nowrap text-malibu-200">
        Website
      </span>
    </a>

    <div class="flex items-center justify-end">
      <button
        id="menu-toggle"
        type="button"
        class="inline-flex items-center p-2 ml-3 text-sm text-malibu-200 rounded-lg hover:bg-primary-bg-50 focus:outline-none focus:ring-2 focus:ring-gray-200 md:hidden"
      >
        <span class="sr-only">Open main menu</span>
        <!-- Hamburger icon -->
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h16m-7 6h7"
          />
        </svg>
      </button>
    </div>

    <div
      class="w-full md:block md:w-100 hidden "
      id="mobile-menu"
    >
      <ul class="flex justify-center flex-col mt-4 md:flex-row md:space-x-8 md:mt-0 md:text-sm font-semibold text-primary-bg-50">
        <li>
          <a
            href="#"
            class="block py-2 pr-4 pl-3 md:p-0 text-center hover:bg-green-500 hover:text-primary-bg-50 md:hover:text-green-500 md:hover:bg-transparent rounded-lg"
            aria-current="page"
          >
            Inicio
          </a>
        </li>
        <li>
          <a
            href="#"
            class="block py-2 pr-4 pl-3 md:p-0 text-center hover:bg-green-500 hover:text-primary-bg-50 md:hover:text-green-500 md:hover:bg-transparent rounded-lg"

          >
            Acerca de 
          </a>
        </li>
        <li>
          <a
            href="#"
            class="block py-2 pr-4 pl-3 md:p-0 text-center hover:bg-green-500 hover:text-primary-bg-50 md:hover:text-green-500 md:hover:bg-transparent rounded-lg"

          >
            Servicios
          </a>
        </li>
       
        <li>
          <a
            href="#"
            class="block py-2 pr-4 pl-3 md:p-0 text-center hover:bg-green-500 hover:text-primary-bg-50 md:hover:text-green-500 md:hover:bg-transparent rounded-lg"
          >
            Contacto
          </a>
        </li>

        
      </ul>
    </div>
    
  </div>
</nav>

<script is:inline>
  const menuToggle = document.getElementById('menu-toggle');
  const mobileMenu = document.getElementById('mobile-menu');
  document.addEventListener('click', function (event) {
    const isClickInside = menuToggle.contains(event.target) || mobileMenu.contains(event.target);

    if (!isClickInside) {
      mobileMenu.classList.add('hidden');
    }
  })
  menuToggle.addEventListener('click', function () {
    mobileMenu.classList.toggle('hidden');
  })
</script>
